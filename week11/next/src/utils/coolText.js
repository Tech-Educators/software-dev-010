export const hackerText = text => {
    const arr = text.split()
    const store = []
    for (let i = 0; i < text.length; i++) {
        const newArray = new Array(text.length)
        
    }
}

hackerText('dog')