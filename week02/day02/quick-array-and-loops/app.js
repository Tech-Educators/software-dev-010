
// a collection of things - mixed data types are allowed, 2d arrays, functions ect. everything. 

let myArray = ['sam', 32, ['somethings', true]]

// arrays are zero-indexed

console.log(myArray[0]);
console.log(myArray[myArray.length - 1]) // give me the last item in the array.

// can use variables
let x = 2;
myArray[x]

myArray[5-3]

myArray[x][1]
// 


// loops .

let testArray = [1,2,3,4,5]

// intialize our tracker var
for (let i = 0; i < testArray.length; i++) {
    console.log(testArray[i])
}

// try looping rhough backwards 

// 

let array = ['blog 1', 'blog2']

// loop through

// creat h2's

// generate onto page. 
